/* 
====================================================================================
🔰 Type Guard using `instanceof` 
====================================================================================

📌 `instanceof` হলো TypeScript/JavaScript এর একটি built-in operator, 
যা দিয়ে আমরা কোনো object কোন class থেকে তৈরি হয়েছে সেটি check করতে পারি।

➡️ অর্থাৎ: object টি যদি ঐ class এর instance হয় → true
➡️ না হলে → false

📌 কখন ব্যবহার করব?
----------------------------------
যখন multiple class ভিত্তিক object নিয়ে কাজ করি এবং 
প্রতিটা class এর আলাদা আলাদা property বা method থাকে— 
তখন কোন object কোন class এর তা check করার জন্য `instanceof` ব্যবহার করা হয়।

====================================================================================
🔰 Basic Example
====================================================================================
*/

class Car {
  drive() {
    console.log("Driving a car...");
  }
}

class Bike {
  ride() {
    console.log("Riding a bike...");
  }
}

// 👉 এখন আমরা একটি function লিখব যা check করবে কোন object কোন class এর instance
function useVehicle(vehicle: Car | Bike) {
  // 🔍 Type Guard using instanceof
  if (vehicle instanceof Car) {
    // এখানে TypeScript নিশ্চিত যে 'vehicle' একটি Car
    vehicle.drive(); // Car এর method
  } else if (vehicle instanceof Bike) {
    // এখানে TypeScript নিশ্চিত যে 'vehicle' একটি Bike
    vehicle.ride(); // Bike এর method
  }
}

const car = new Car();
const bike = new Bike();

useVehicle(car);   // Driving a car...
useVehicle(bike);  // Riding a bike...



/*
====================================================================================
🔰 আরেকটু অ্যাডভান্সড Example (Properties সহ)
====================================================================================
*/

class Dog {
  type = "dog";
  bark() {
    console.log("Dog is barking 🐶");
  }
}

class Cat {
  type = "cat";
  meow() {
    console.log("Cat is meowing 🐱");
  }
}

function interactWithAnimal(animal: Dog | Cat) {
  if (animal instanceof Dog) {
    // Dog এর special behavior
    console.log("Dog detected!");
    animal.bark();
  } else if (animal instanceof Cat) {
    // Cat এর special behavior
    console.log("Cat detected!");
    animal.meow();
  }
}

interactWithAnimal(new Dog());
interactWithAnimal(new Cat());



/*
====================================================================================
🔰 instanceof কিভাবে কাজ করে?
====================================================================================

➡️ object instanceof Class
মানে: object এর prototype chain এ Class.prototype আছে কিনা check করে।

উদাহরণ:
car instanceof Car → true
car instanceof Bike → false

====================================================================================
🔰 কখন `instanceof` ব্যবহার করা উচিত নয়?
====================================================================================

❌ যখন object plain object ({}), type alias বা interface ভিত্তিক হয়  
❌ যখন object JSON থেকে আসে  
❌ primitive টাইপ হলে (string, number etc.)

কারণ instanceof শুধু Class ভিত্তিক instance এর জন্য কাজ করে।

====================================================================================
🔰 কখন `instanceof` ব্যবহার করা উচিত?
====================================================================================

✔ যখন multiple class দিয়ে polymorphism করা হয়  
✔ যখন method/property class অনুযায়ী আলাদা আলাদা হয়  
✔ যখন runtime এ object এর type নির্ধারণ দরকার হয়  

====================================================================================
🔰 Summary (Short Notes — Revised এর জন্য দরকারি)
====================================================================================

★ instanceof দিয়ে runtime এ object কোন class এর instance তা চেক করা হয়  
★ শুধুমাত্র class দিয়ে তৈরি object এর জন্য কাজ করে  
★ polymorphism, OOP এ খুবই useful  
★ prototype chain ভিত্তিক check করে  
====================================================================================
*/
