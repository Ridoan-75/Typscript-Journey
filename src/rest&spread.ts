/*
===========================================================
💥 REST OPERATOR (...) এবং SPREAD OPERATOR (...) — সম্পূর্ণ ব্যাখ্যা
===========================================================

🧠 ভূমিকা:
-----------------
TypeScript (এবং JavaScript) এ "..." তিনটি ডট দুটি ভিন্ন কাজে ব্যবহার হয় —
➡️ Rest Operator
➡️ Spread Operator

দুইটার চেহারা এক (তিনটা ডট), কিন্তু কাজ সম্পূর্ণ আলাদা!

⭐ REST OPERATOR → একাধিক মান (values) কে একত্রে একটি variable-এ "সংগ্রহ" করে।
⭐ SPREAD OPERATOR → কোনো array/object এর ভেতরের মানগুলোকে "ছড়িয়ে" বা "unpack" করে দেয়।

এখন চল একদম A থেকে Z সব কিছু দেখি 👇
*/


// =======================================================
// 🔹 PART 1: REST OPERATOR (...)
// =======================================================
//
// 📘 REST Operator সাধারণত function parameter বা destructuring এ ব্যবহার হয়।
// এটি multiple values কে একত্র করে একটা single array বানিয়ে ফেলে।
//


// ✅ Example 1: REST Operator in Function Parameters
function sumAllNumbers(...numbers: number[]): number {
  // এখানে ...numbers মানে হলো — যত argument পাঠানো হবে, সবগুলোই numbers নামের একটি array তে চলে যাবে।
  // যেমন: sumAllNumbers(10, 20, 30, 40) => numbers = [10, 20, 30, 40]

  let total: number = 0;

  for (let num of numbers) {
    total += num; // প্রতিটা সংখ্যা যোগ করছি
  }

  return total; // সব সংখ্যার যোগফল ফেরত দিচ্ছে
}

console.log(sumAllNumbers(5, 10, 15, 20)); // Output: 50



// ✅ Example 2: REST Operator in Array Destructuring
const [first, second, ...remainingNumbers] = [1, 2, 3, 4, 5, 6];
// এখানে first = 1, second = 2, আর বাকি সব সংখ্যা remainingNumbers array তে যাবে => [3, 4, 5, 6]

console.log(first);           // 1
console.log(second);          // 2
console.log(remainingNumbers); // [3, 4, 5, 6]



/*
===============================================
🧩 REST OPERATOR এর মূল পয়েন্ট:
===============================================

1️⃣ Function Parameter এ ... ব্যবহার করলে সব argument array আকারে আসে।
2️⃣ Destructuring এ ... ব্যবহার করলে বাকি সব element গুলোকে ধরে রাখে।
3️⃣ REST Operator শুধুমাত্র শেষের parameter হিসেবে ব্যবহার করা যায়।
   ❌ function example(a, ...b, c) — এটা invalid!
   ✅ function example(a, b, ...c) — এটা valid!
*/


// =======================================================
// 🔹 PART 2: SPREAD OPERATOR (...)
// =======================================================
//
// 📘 SPREAD Operator কোনো array/object এর উপাদানগুলোকে "ছড়িয়ে দেয়" বা "unpack" করে।
// অর্থাৎ, এটি array বা object এর মানগুলোকে আলাদা আলাদা করে বের করে আনে।
//


// ✅ Example 1: Spread Operator with Arrays
const array1: number[] = [1, 2, 3];
const array2: number[] = [4, 5, 6];

// দুইটা array merge করা হলো spread operator দিয়ে
const mergedArray: number[] = [...array1, ...array2];

console.log(mergedArray); // [1, 2, 3, 4, 5, 6]



// ✅ Example 2: Copying Arrays using Spread
const originalArray: number[] = [10, 20, 30];
const copiedArray: number[] = [...originalArray]; // নতুন একটি array তৈরি হলো (shallow copy)

copiedArray.push(40);

console.log(originalArray); // [10, 20, 30] → মূল array অপরিবর্তিত
console.log(copiedArray);   // [10, 20, 30, 40]



// ✅ Example 3: Spread Operator with Objects
const person = { name: "Ridoan", age: 22 };
const additionalInfo = { country: "Bangladesh", dept: "CSE" };

// দুইটা object merge করা হলো spread দিয়ে
const fullProfile = { ...person, ...additionalInfo };

console.log(fullProfile);
// Output: { name: 'Ridoan', age: 22, country: 'Bangladesh', dept: 'CSE' }



// ✅ Example 4: Spread Operator in Function Arguments
function multiply(a: number, b: number, c: number): number {
  return a * b * c;
}

const numbersArray: number[] = [2, 3, 4];

// এখানে আমরা array টাকে spread করে তিনটা argument হিসেবে পাঠাচ্ছি
console.log(multiply(...numbersArray)); // Output: 24



/*
===============================================
🧩 SPREAD OPERATOR এর মূল পয়েন্ট:
===============================================

1️⃣ কোনো array/object কে ছড়িয়ে (expand) দিতে ব্যবহৃত হয়।
2️⃣ Function call করার সময় argument হিসেবে array spread করা যায়।
3️⃣ Shallow copy (এক স্তরের কপি) তৈরিতে খুব উপযোগী।
4️⃣ একাধিক array/object merge করতে সহজ পদ্ধতি।

*/


// =======================================================
// 🔹 PART 3: REST vs SPREAD তুলনা (Comparison)
// =======================================================
//
// অনেকেই দুইটা নিয়ে কনফিউস হয়, তাই নিচে সহজভাবে পার্থক্যটা দেখা যাক 👇
//

/*
--------------------------------------------------------
| বিষয় (Feature)       | REST Operator       | SPREAD Operator      |
|----------------------|---------------------|-----------------------|
| মূল কাজ              | সংগ্রহ করা (Collect) | ছড়িয়ে দেয়া (Expand)  |
| অবস্থান               | Function Parameter  | Function Call, Array/Object |
| ফলাফল (Result)        | Array তৈরি করে      | Array/Object ছড়িয়ে দেয় |
| ব্যবহার উদাহরণ        | function(...args)   | myFunc(...array)      |
| মানে                  | "Gather values"     | "Spread values"       |
--------------------------------------------------------
*/



/*
===========================================================
📘 সারসংক্ষেপ (Summary):
===========================================================

🔹 REST Operator:
-----------------
👉 একাধিক মানকে array আকারে "সংগ্রহ" করে।
👉 Function parameter বা destructuring এ ব্যবহৃত হয়।

🔹 SPREAD Operator:
-------------------
👉 কোনো array/object এর মানগুলোকে "ছড়িয়ে" দেয়।
👉 Function call, array merge, object merge ইত্যাদিতে ব্যবহৃত হয়।

🔹 মনে রাখবে:
----------------
REST Operator — values কে একত্রে নেয় (Collect করে)
SPREAD Operator — values কে ছড়িয়ে দেয় (Expand করে)

দুইটা দেখতে এক রকম হলেও কাজ একদম বিপরীত।
এবং দুটোই TypeScript এর খুবই গুরুত্বপূর্ণ Concept।
*/
