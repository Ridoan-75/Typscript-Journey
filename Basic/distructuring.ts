/*
=============================================================
💥 DESTRUCTURING IN TYPESCRIPT
=============================================================

🧠 ভূমিকা:
----------------
TypeScript (এবং JavaScript) এ “Destructuring” হলো এমন একটা পদ্ধতি,
যার মাধ্যমে আমরা কোনো array বা object থেকে এক বা একাধিক মান খুব সহজে আলাদা করে ভ্যারিয়েবলে রাখতে পারি।

আগে যেভাবে করতাম:
-------------------
let arr = [10, 20, 30];
let a = arr[0];
let b = arr[1];
let c = arr[2];

👉 এখন আমরা Destructuring ব্যবহার করে এক লাইনেই এই কাজগুলো করতে পারি।

*/


// ===========================================================
// 🔹 PART 1: ARRAY DESTRUCTURING (Array থেকে মান আলাদা করা)
// ===========================================================

const numbers: number[] = [10, 20, 30, 40, 50];

// ✅ পুরনো পদ্ধতি:
// let a = numbers[0];
// let b = numbers[1];
// let c = numbers[2];

// ✅ নতুন পদ্ধতি (Destructuring):
const [a, b, c] = numbers;

// এখন a = 10, b = 20, c = 30
console.log(a, b, c); // Output: 10 20 30


/*
-----------------------------------------
📘 ব্যাখ্যা:
-----------------------------------------
👉 এখানে [a, b, c] = numbers মানে হচ্ছে —
numbers array এর প্রথম তিনটা মান ক্রমানুসারে a, b, c তে বসবে।

অর্থাৎ:
numbers[0] → a
numbers[1] → b
numbers[2] → c
*/


// ✅ Extra elements বাদ দিতে চাইলে
const [first, , third] = [1, 2, 3, 4];
// এখানে দ্বিতীয় মানটা বাদ দেয়া হয়েছে (একটা কমা ব্যবহার করে)
console.log(first, third); // Output: 1 3


// ✅ Default value সেট করা (যদি array তে মান না থাকে)
const [x = 100, y = 200, z = 300] = [10, 20];
// এখানে z এর মান array তে নেই, তাই default 300 ব্যবহার হবে
console.log(x, y, z); // Output: 10 20 300


// ✅ REST Operator এর সাথে Destructuring
const [p, q, ...restValues] = [5, 10, 15, 20, 25];
console.log(p);           // 5
console.log(q);           // 10
console.log(restValues);  // [15, 20, 25]

/*
-----------------------------------------
📘 সারাংশ (Array Destructuring):
-----------------------------------------
1️⃣ একাধিক মান একসাথে আলাদা করা যায়।
2️⃣ ফাঁকা কমা ব্যবহার করে কিছু মান skip করা যায়।
3️⃣ Default value সেট করা যায়।
4️⃣ Rest Operator ব্যবহার করে বাকি মানগুলো ধরা যায়।
*/



// ===========================================================
// 🔹 PART 2: OBJECT DESTRUCTURING (Object থেকে মান আলাদা করা)
// ===========================================================

const person = {
  name: "Ridoan",
  age: 22,
  country: "Bangladesh",
  department: "CSE"
};

// ✅ পুরনো পদ্ধতি:
// let name = person.name;
// let age = person.age;
// let country = person.country;

// ✅ নতুন পদ্ধতি (Destructuring):
const { name, age, country } = person;
console.log(name, age, country);
// Output: Ridoan 22 Bangladesh


/*
-----------------------------------------
📘 ব্যাখ্যা:
-----------------------------------------
👉 Object Destructuring এ property নামের ভিত্তিতে মান আলাদা করা হয়।
👉 অর্থাৎ property name = variable name হতে হবে।
*/


// ✅ Variable এর নাম আলাদা দিতে চাইলে (rename)
const { department: dept } = person;
// এখানে object এর 'department' প্রপার্টি টাকে আমরা 'dept' নামে নিচ্ছি
console.log(dept); // Output: CSE


// ✅ Default value সেট করা
const { hobby = "Programming" } = person;
// যদি hobby প্রপার্টি না থাকে, তাহলে default মান "Programming" হবে
console.log(hobby); // Output: Programming


// ✅ Nested Object Destructuring
const student = {
  id: 101,
  info: {
    fullName: "Md Ridoan",
    address: {
      city: "Dhaka",
      zip: 1205,
    },
  },
};

const {
  info: {
    fullName,
    address: { city, zip },
  },
} = student;

console.log(fullName); // Md Ridoan
console.log(city);     // Dhaka
console.log(zip);      // 1205


/*
-----------------------------------------
📘 সারাংশ (Object Destructuring):
-----------------------------------------
1️⃣ Property নাম অনুযায়ী মান আলাদা করা হয়।
2️⃣ Variable rename করা যায়।
3️⃣ Default value দেয়া যায়।
4️⃣ Nested object থেকে মান বের করা যায়।
*/



// ===========================================================
// 🔹 PART 3: FUNCTION PARAMETER DESTRUCTURING
// ===========================================================
//
// Function parameter এর মধ্যেও Destructuring ব্যবহার করা যায়,
// যাতে কোড আরও ক্লিন ও readable হয়।
//


type Employee = {
  empName: string;
  empAge: number;
  empDept: string;
};

// ✅ সাধারণভাবে:
function showEmployeeInfo(employee: Employee): void {
  console.log(`Name: ${employee.empName}, Age: ${employee.empAge}, Dept: ${employee.empDept}`);
}

const emp1: Employee = { empName: "Sabbir", empAge: 25, empDept: "Software" };
showEmployeeInfo(emp1);

// ✅ Destructuring ব্যবহার করে:
function displayEmployee({ empName, empAge, empDept }: Employee): void {
  console.log(`Name: ${empName}, Age: ${empAge}, Dept: ${empDept}`);
}

displayEmployee(emp1);

/*
-----------------------------------------
📘 ব্যাখ্যা:
-----------------------------------------
👉 Function parameter এর মধ্যেই Destructuring করলে
   আলাদা করে employee.empName, employee.empAge লেখার দরকার হয় না।
👉 কোড অনেক বেশি সংক্ষিপ্ত ও readable হয়।
*/



// ===========================================================
// 🔹 PART 4: COMBINED USE — Array + Object Destructuring
// ===========================================================
//
// অনেক সময় array এর মধ্যে object থাকে, বা object এর মধ্যে array থাকে।
// তখন দুইটা একসাথে ব্যবহার করা যায়।
//

const users = [
  { id: 1, username: "Ridoan", country: "Bangladesh" },
  { id: 2, username: "Sabbir", country: "India" },
  { id: 3, username: "Arafat", country: "Nepal" },
];

for (const { id, username } of users) {
  console.log(`UserID: ${id}, Username: ${username}`);
}
// Output:
// UserID: 1, Username: Ridoan
// UserID: 2, Username: Sabbir
// UserID: 3, Username: Arafat


/*
=============================================================
📘 সারসংক্ষেপ (Summary):
=============================================================

🔹 Destructuring কী:
--------------------
👉 কোনো array বা object থেকে এক বা একাধিক মান সহজে আলাদা করে নেওয়ার প্রক্রিয়া।

🔹 Array Destructuring:
------------------------
✅ Syntax: const [a, b] = array;
✅ মান Index অনুযায়ী আসে।
✅ Default value, skipping, rest operator সবই ব্যবহার করা যায়।

🔹 Object Destructuring:
------------------------
✅ Syntax: const {prop1, prop2} = object;
✅ মান property name অনুযায়ী আসে।
✅ Variable rename, default value, nested destructuring সম্ভব।

🔹 Function Parameter Destructuring:
------------------------
✅ Function argument হিসেবেই destructuring করা যায়।
✅ কোড ছোট ও readable হয়।

Destructuring এর সাহায্যে কোড অনেক বেশি সুন্দর, পরিষ্কার এবং maintainable হয়।
এটি TypeScript এর একদম বেসিক কিন্তু অত্যন্ত শক্তিশালী ফিচার।
*/
